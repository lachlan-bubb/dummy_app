[tool.poetry]
name = "my_dash_app"
version = "0.1.0"
description = "My Dash App"
authors = ["Lachlan Bubb"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
python = "^3.8"
dash = "^2.0.0"